<%= form_tag(ideas_index_path, method: :get, class: 'global-search') do %>
  <div class="input-inline">
    <label class="input-group__label sr-only" for="global-search-input">Looking for inspiration?</label>
    <div class="input-row">
      <%= text_field_tag('q', 'e.g. Swimming with dolphins', class: 'input-row__field') %>
      <%= button_tag('Search', class: 'btn btn--inline btn--primary') %>
    </div>
  </div>
<% end %>
